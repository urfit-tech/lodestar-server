import { ExampleRunner } from './example.runner';
import { InvoiceRunner } from './invoice.runner';
import { TriggerRunner } from './trigger.runner';
import { PorterRunner  } from './porter.runner' ;

export const RunnerType = {
  ExampleRunner,
  InvoiceRunner,
  TriggerRunner,
  PorterRunner
};
